# План на неделю

Отмечай выполненное. В конце каждого дня — рабочий коммит и запись в «Журнал».

## День 1 — ядро

- [x] Каркас проекта: `pyproject.toml`, `uv`, `ruff`, `pytest`, структура пакета
- [x] Модель пород: датакласс + таблица из `docs/domain.md` + загрузка из YAML
- [x] Единицы измерения: мм внутри, форматтеры мм/дюймы на границе
- [x] Операции `Glue`, `Crosscut`, `StandOnEnd`, `Cut`, `Assemble`, `Crop`
- [x] Применение программы → набор полигонов с породами
- [x] Учёт kerf и припусков на строгание
- [x] Тесты на каждую операцию + тест «шахматка воспроизводится из программы»

**Готово, если:** программа из 4 операций даёт корректный список полигонов,
а `pytest` зелёный.

## День 2 — узор видно

- [x] Происхождение ячейки: щит, рейка, смещение по длине рейки, порода
- [x] Палитра породы (Р12) + `boardforge swatches` для сверки цветов
- [x] SVG-рендер полигонов с заливкой по породе, швами и режимом как параметром
- [x] Процедурная текстура торца: годичные кольца, сердцевинные лучи, сид на рейку
- [x] Уровни детализации и бенчмарк доски 30×20
- [x] Два слоя рендера: структура быстро, текстура догоняет
- [x] Каркас FastAPI + Jinja + HTMX, страница с превью
- [x] Редактирование состава щита
- [x] Настройка габаритов доски обратной задачей + переключатель мм/дюймы
- [x] Валидатор в интерфейсе: разбор словами, с привязкой к операции
- [x] Сохранение/загрузка проекта (JSON)
- [x] `out/contact-sheet.svg` — все доски и породы одной страницей

**Готово, если:** в браузере можно собрать простую доску и сохранить её.

## День 3 — углы (самый рискованный день)

- [x] Резы под произвольным углом
- [x] Ёлочка и шеврон
- [x] Кубы: разобраны, перенесены на День 4. Бесшовные не строятся (Р22),
      выразим вариант с волосяной линией на каждой грани
- [x] Третья склейка в общем виде
- [x] Валидатор изготовимости с человекочитаемым разбором
- [x] Бенчмарк производительности + кэш по хешу программы
- [x] Контактный лист с угловыми узорами

**Готово, если:** кубы и ёлочка собираются, валидатор объясняет проблемы словами.

## День 4 — генерация

- [x] Кубы: вариант с волосяной линией на каждой грани (Р22). Построены, но
      **из двух щитов-близнецов, а не из одного** — Р23. Условие
      `thickness_mm = A·√3/2` в силе, управляет шагом четвёртого (невидимого)
      семейства швов
- [x] Решёточный подбор сдвигов функцией ядра (`core/lattice.py`): кратчайший
      вектор решётки вдоль оси реза, согласование двух условий, явное
      «решения нет» вместо приближения
- [x] Случайный генератор в пространстве программ (`core/generate.py`)
- [x] Эволюционный режим с фитнес-функцией (`core/fitness.py`)
- [x] Библиотека шаблонов — 14 узоров (`core/library.py`)
- [x] Импорт изображения → квантование в CIELAB по доступным породам
      (`core/imaging.py`, чтение растра в `io/images.py` без зависимостей)
- [x] Сид и воспроизводимость: один сид — один узор

**Готово, если:** кнопка «удиви меня» выдаёт разнообразные изготовимые узоры.

## День 5 — цех

- [x] Карта раскроя с номерами деталей (`calc/cutlist.py`): рейки `A1`, полосы
      `A-01`, все стадии реза, у каждой полосы место в склейке
- [x] Расчёт материала с разбивкой потерь: пропил / строгание / обрезка.
      Заодно найдены и закрыты два дефекта расчёта — см. журнал
- [x] Расчёт отходов и себестоимости, редактируемые цены пород
      (`calc/estimate.py`)
- [x] Ч/б-чертежи с буквенными обозначениями пород и размерами
      (`render/blueprint.py`, режим `BLUEPRINT` в `STYLES`)
- [x] Одномерная оптимизация раскроя из досок стандартной длины
      (`calc/nesting.py`): FFD плюс возмущённый порядок, бюджет по времени
- [x] Клей, вес доски, расход масла. Струбцины не считаем: их число зависит
      от длины щита и от того, что лежит в мастерской, а не от программы
- [x] Предупреждения по породам (`calc/warnings.py`), порог по короблению
      подобран по данным: 3.0 п.п., срабатывает на 3 узорах библиотеки из 14
- [x] Распечатка целиком (`io/report.py` + `boardforge workshop`): тот же HTML
      уйдёт в PDF на Дне 6

**Готово, если:** по распечатке можно идти в мастерскую и пилить.

## День 6 — впечатление

- [ ] 3D-превью: `trimesh` → `.glb` → `<model-viewer>`, фаска и покрытие маслом
- [ ] PDF-инструкция: пошагово, с картинками каждого этапа. Страница уже есть
      (`io/report.py`), нужен принтер: WeasyPrint или headless-браузер
- [x] Предупреждения: коробление, поры, аллергены — закрыто на Дне 5.
      Минимальная ширина детали была закрыта на Дне 3 (`core/safety.py`)
- [ ] Undo/redo
- [ ] Полировка UX: горячие клавиши, пустые состояния, понятные ошибки

**Готово, если:** человек, не читавший код, проходит путь от идеи до PDF.

**Проверено заранее (в начале Дня 3).** `trimesh` ставится и импортируется.
`weasyprint` ставится, но падает при импорте: `OSError: cannot load library
'libgobject-2.0-0'` — колесо не тянет нативные Pango/GLib, нужен системный
GTK3-рантайм. Рантайм ставится отдельно, вне проекта.

**Запасной путь для PDF, если GTK не заработает:** печатать тот же HTML
headless-браузером (CDP `Page.printToPDF`). Контракт «один вектор идёт и на
экран, и в печать» при этом сохраняется — меняется только принтер. Цена:
браузер как зависимость окружения вместо нативных библиотек. Не реализуем,
пока WeasyPrint не признан безнадёжным.

## День 7 — сдача

- [ ] CLI-режим: генерация, экспорт, расчёты из терминала
- [ ] «Столярный демон» — интерактивный режим в Rich
- [ ] README с картинками и честным описанием границ
- [ ] 5–6 демо-проектов в комплекте
- [ ] Прогон всех тестов, проверка на чистой машине
- [ ] Буфер на то, что поехало

## Журнал

<!-- Claude: дописывай сюда одну строку за сессию: что сделано, что сломано, что дальше -->

- **День 1.** `CLAUDE.md` и `domain.md` приведены к `decisions.md`; ядро (единицы,
  породы, DSL, геометрия, валидатор) и обратный ход припусков в `calc/` готовы,
  93 теста зелёные. Открыто: цвета пород подобраны на глаз, второй `Glue`
  запрещён валидатором (понадобится на Дне 3 для кубов), `flipped` принимается,
  но на геометрию не влияет. Дальше — День 2, SVG и веб.
- **Р9–Р12, перед Днём 2.** Состояние программы — именованный набор заготовок;
  `schema_version` 2 и миграция с 1. `derivation.py` отвечает, из какого щита
  берётся ряд и чего не хватает, если ни из какого. Защита предусловия
  `stand_on_end` перенесена в геометрию. Припуск на обрезку разделён по осям
  (Р11): эталон подешевел с +95% до +69%. Property-тесты на резы (`hypothesis`)
  и снапшот контрольной сметы. 154 теста зелёные. Открыто: Р12 (палитра
  породы) — за Днём 2 вместе с текстурой.
- **День 2, первая половина.** Слой рендера. Ячейка несёт происхождение
  (`Origin`: щит, рейка, смещение по длине рейки, порода) — `Piece.species`
  теперь производная от него, а `glue` получил имя щита. Р12 закрыт: палитра
  вместо цвета, все 12 пород помечены `verified: false`, сверка через
  `boardforge swatches`. Свой SVG-writer (`render/canvas.py`), режим рендера —
  запись в `STYLES`, не ветка: есть `preview` и `flat`, ч/б чертёж Дня 5
  добавляется своей `repaint`. Текстура сеет от рейки, дрейф вдоль неё
  абсолютный (мм), а не в долях расстояния до сердцевины, иначе почти прямые
  кольца на соседних срезах разъезжались на десяток колец. Бенчмарк 30×20 = 600
  ячеек: 0.9 с и 680 КБ при 2 px/мм, порог детализации срезает до 56 КБ. 207
  тестов зелёные. Открыто: CLI на `argparse`, а не Typer (нет в окружении);
  демо-программы лежат в `cli/examples.py` до библиотеки узоров Дня 4; `flipped`
  на текстуру пока не влияет. Дальше — веб-оболочка.
- **День 2, после первой ручной сверки палитр.** Сверка отвергла лучи: ровный
  шаг и ровная длина давали решётку, видную с двух метров. Лучи переписаны —
  промежуток, длина и толщина разыгрываются от сида рейки, плотность падает
  с радиусом сама (веер), при отдалении рисунок сливается в тон (Р16). В палитру
  добавлены `ray_width_mm` и `latewood_fraction`; узкая поздняя зона рисуется
  темнее широкой при том же контрасте, чем и выражается венге. Значения всех
  12 пород пересчитаны, `verified: false` оставлен. Найден и закрыт дефект:
  текстура не разворачивалась вместе с деталью — `Piece` получил `Orientation`,
  и `reversed`/`flipped` наконец видно в превью (Р15); заодно `flipped` дошёл
  до геометрии. CLI зафиксирован на `argparse` (Р17). Снапшот обновлён,
  223 теста зелёные. Открыто: демо-программы всё ещё в `cli/examples.py`.
  Заведён отдельный набор `DIAGNOSTICS` — стенды, а не узоры: доска собрана
  так, чтобы одно свойство рендера было видно глазом и ничем другим не
  объяснялось. Первый стенд `reversed-check`: один ряд из одной рейки, чётные
  ячейки прямые, нечётные развёрнуты. Ряд, а не щит, потому что разворот детали
  переставляет её ячейки по вертикали — в многорядном щите соседние столбцы
  пришли бы из разных реек, и разницу текстуры можно было бы списать на это,
  а не на поворот. Рисуется по `--diagnostics`, в наборе шаблонов его нет:
  порог «узор — это не десяток ячеек» держится для библиотеки Дня 4.
- **День 2, вторая половина.** Веб-оболочка. Рендер разнесён на два слоя
  (структура 78 мс / 60 КБ, текстура 286 мс / 749 КБ на 600 ячейках); контракт
  между ними — структура со вставленной текстурой побайтово равна полному
  документу. В UI структура приходит сразу, текстуру браузер просит сам через
  200 мс. FastAPI + Jinja + HTMX: обмен фрагментами через `hx-swap-oob`, htmx
  лежит локально, формы разбираются `parse_qsl` без `python-multipart`.
  Обратная задача по габаритам (`core/fitting.py`) переписывает программу и
  **меряет** попадание исполнением, а не формулой; Р8 уточнён — оси там названы
  наоборот (Р18). Валидатор виден в правой колонке с привязкой к номеру
  операции. Проект сохраняется и открывается по пути на диске. `contact-sheet`
  собирает все доски и породы одной страницей — и сразу вскрыл настоящий баг:
  у вложенных документов совпадали `id` обрезки, и `clipPath` первой доски
  обрезал текстуру остальных. 253 теста зелёные. Открыто: `uv run pytest`
  падает на уборке `%TEMP%` из-за битого junction `pytest-current` (лечится
  одной командой, см. отчёт); обратная задача умеет только простые программы —
  на Дне 3 понадобится общий случай.
- **День 3.** Углы. `SliceResult` вместо кортежа: к недорезу добавились
  измеренная площадь отхода (при угловом резе клин из недореза не выводится)
  и `placements_mm` — где полоса лежала до нормализации, без этого генераторы
  не соберут узор обратно. Property-тесты уровня программы на нескольких щитах,
  проверены мутациями: занизь отход на 1%, сдвинь полосы на полшага, убери
  масштаб у `StandOnEnd` — все три ловятся. Р21: `flipped` стал настоящим
  зеркалом в плане (на прямоугольной ячейке тождество, потому обратно
  совместимо). `core/patterns.py` — генераторы шеврона и ёлочки; главная
  находка дня в том, что поправка на зеркало **накапливается**, а не чередуется,
  и сворачивается по периоду узора целиком, вместе с опорным членом, — иначе
  щит воспроизводит повёрнутый прямоугольник, то есть лестницу. Кубы разобраны
  и отправлены на День 4: бесшовные не строятся, и вариант «одна грань из трёх»
  тоже — наклонные границы внутри столбца требуют собственных резов (Р22,
  схема `out/cubes-verdict.svg`). `core/safety.py`: клин опасен не шириной,
  а носом `min(θ, 90°−θ)`; ячейка при этом не деталь — её склеили несколько
  операций назад, — поэтому у неё порог косметический, а не защитный. Кэш по
  префиксу программы: ключ сам префикс, операции заморожены и хешируются.
  331 тест зелёный. Открыто: обрезка до прямоугольника требует отбраковки трёх
  крайних полос с каждой стороны, иначе прямоугольника нет вовсе — на угловом
  узоре в отход уходит около половины щита, и на Дне 5 это должно попасть
  в смету честно.
- **День 4.** Генераторы. Кубы построены — и по дороге выяснилось, что Р22 ошибался
  в главном: из одного щита мозаика не собирается **ни при каких** сдвигах, это
  арифметика (Р23). Строится из двух щитов-близнецов, набранных одним циклом
  со сдвигом на одну рейку; мульти-щит Р9 впервые понадобился по существу.
  Проверка не на глаз: эталонная мозаика строится в тесте заново, проекцией
  кубической решётки, и доска ложится на неё точка в точку. `core/lattice.py`
  отвечает на вопрос Р22 «когда сдвиг сведёт узор» арифметикой и умеет сказать
  «решения нет» вместо приближения; у кубов условий два, и они согласуются.
  Библиотека — 14 параметрических шаблонов, каждый со своим тестом на схождение:
  ортогональные объявляют период трансляции, угловые проверяются по швам.
  Тест на попарную различимость держит порог «не двенадцать вариаций шахматки».
  Фитнес-функция из пяти мер; экономичность сразу показала цену углов: шахматка
  0.72, шеврон 0.30, кубы 0.05. Генератор по сиду детерминирован и прогоняет
  результат через валидатор и проверку изготовимости. Импорт картинки квантует
  в CIELAB и **раскладывает столбцы по немногим щитам** — иначе результат нельзя
  склеить; точность сообщается честно. Растр читается своим кодом на `zlib`,
  зависимость не добавлена, и в пустом `io/` наконец что-то появилось.
  Найдено и закрыто заодно: `unary_union` оставлял в контуре детали щели нулевой
  ширины, и `outline_min_angle_deg` находил там нос в 0° — валидатор Дня 3 врал
  ложной ошибкой; лечится округлением контура до нанометра. 561 тест зелёный.
  Открыто: `.pytest-tmp` в репозитории повторил судьбу `%TEMP%\pytest-of-*`
  (битые ACL, `takeown` без прав администратора не берёт) — basetemp переехал
  на `.pytest-run`; кубы дороги настолько, что для Дня 5 это отдельный разговор
  про смету; `windmill` — единственный шаблон, где работает `reversed`.
- **День 5.** Цех. Породы сверены вручную, ожидания в тестах доведены до конца,
  а числа справочника зафиксированы таблицей `REFERENCE_DATA`: с этого дня на
  них стоят смета, вес и порог по короблению, и опечатка в цифре врёт в рублях.
  Каталог временных файлов pytest перестал ломаться, и не переездом, а по
  причине: с Python 3.13 `os.mkdir(mode=0o700)` на Windows **применяет** режим
  и ставит защищённый дескриптор, доступный только создателю, а `--basetemp`
  заставляет pytest делать это по постоянному имени. Один прогон под другим
  токеном — и каталог мёртв навсегда. Лечение: каталоги с постоянными именами
  создаём сами обычным `mkdir` (они наследуют права репозитория), pytest
  возвращается к нумерованным листьям, которые никогда не переиспользуются.
  Разбор в `conftest.py` в корне. «Удиви меня» доехала до браузера вместе
  с оценками узора, и оценки гаснут сами, как только доску правят руками.
  Карта раскроя перечисляет **все** стадии реза; у углового узора их три,
  и без номеров вторую пачку от первой не отличить. Заодно нашлись два дефекта
  расчёта материала: припуск на строгание считался по **всем** склейкам
  программы, из-за чего щиты-близнецы кубов выходили разной длины, а само
  строгание мерилось по площади готовой доски, а не промежуточного щита —
  то есть занижалось именно там, где узор дорог. Про «половину полос каждого
  щита в комплект на вторую доску» выяснилось, что стадия не та: щиты A и B
  расходуются целиком, а лишним оказывается остаток **последнего** реза —
  измерено, а не предположено, и даёт ровно одну лишнюю доску. Порог по
  короблению 3.0 п.п. подобран по данным с якорем «клён с орехом (2.1) ругаться
  не должен»: срабатывает на 3 узорах из 14, при 2.0 — на одиннадцати.
  Локальный поиск в раскрое пришлось переписать: первая редакция перемешивала
  рейки и тут же сортировала их по убыванию, переставляя только равные по длине,
  то есть взаимозаменяемые, — поиска не было вовсе. 667 тестов зелёные.
  Открыто: цены пород — порядок величины и живут в коде, а не в файле; роспуск
  доски по ширине в раскрой не входит, план даёт нижнюю оценку метража;
  расчёт цеха для кубов стоит полсекунды и держится на кэше в редакторе.
