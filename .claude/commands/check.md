---
description: Проверка перед коммитом
---

Прогони `uv run ruff check .`, `uv run ruff format --check .` и `uv run pytest`.
Почини всё, что упало. Затем покажи `git diff --stat` и предложи текст коммита
одной строкой на русском. Коммить только после моего подтверждения.
